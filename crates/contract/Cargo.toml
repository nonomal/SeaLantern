[package]
name = "sealantern-contract"
version.workspace = true
edition.workspace = true
license = "GPL-3.0-only"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
serde_json = "1"
