#!/usr/bin/env sh

if ! node node_modules/lint-staged/bin/lint-staged.js --version >/dev/null 2>&1; then
  echo "缺少 lint-staged 依赖，请先执行：pnpm install"
  exit 1
fi

node node_modules/lint-staged/bin/lint-staged.js